datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        url: http://prometheus-server.prometheus.svc.cluster.local
        access: proxy
        isDefault: true
persistence:
  enabled: true
#dashboards:
#  default:
#    custom-dashboard:
#      file: /build/guardgress-dashboard.json

