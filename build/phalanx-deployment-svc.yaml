apiVersion: v1
kind: ServiceAccount
metadata:
  name: phalanx
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: pod-reader
rules:
- apiGroups: [""]
  resources: ["services","secrets"]
  verbs: ["get", "watch", "list"]
- apiGroups: ["extensions","networking.k8s.io"]
  resources: ["ingresses",]
  verbs: ["get", "watch", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: pod-reader-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: pod-reader
subjects:
- kind: ServiceAccount
  name: phalanx
  namespace: default
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: phalanx
  name: phalanx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: phalanx
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: phalanx
    spec:
      serviceAccountName: phalanx
      containers:
      - image: docker.io/library/phalanx:1.0.0-dev
        name: phalanx
        ports:
        - containerPort: 443
          name: https
          protocol: TCP
        - containerPort: 80
          name: http
          protocol: TCP
        resources: {}
---
apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: phalanx
  name: phalanx
spec:
  type: NodePort
  ports:
  - port: 443
    name: https
    protocol: TCP
    targetPort: 443
  - port: 80
    name: http
    protocol: TCP
    targetPort: 80
  selector:
    app: phalanx
status:
  loadBalancer: {}
